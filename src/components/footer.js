/** @jsx jsx */
import { jsx } from "theme-ui"

const footer = () => {
  return (
    <div
      sx={{
        width: '100%',
        height: '15px',
        mt: 6,
        bg: 'muted',
        display: 'block',
      }}
    />
  )
}

export default footer