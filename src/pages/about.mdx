---
title: About
---

import { graphql } from "gatsby"
import { BaseStyles } from "theme-ui"

<BaseStyles dangerouslySetInnerHTML={{ __html: props.data.content.edges[0].node.html }} />

export const pageQuery = graphql`
  query {
    content: allMarkdownRemark(filter: {fields: {collection: {eq: "pages"}}, frontmatter: {path: {eq: "about"}}}) {
      edges {
        node {
          html
        }
      }
    }
  }
`